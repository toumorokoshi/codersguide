#/usr/bin/env bash
book sm  # needs gitbook-summary
gitbook build
mv _book = /tmp/_book/
git checkout gh-pages
rm -r *
mv /tmp/_book/* .
git commit -am "build $(date)"
git push origin gh-pages
git checkout master